# Feiyue-Model Training Configuration
# Based on PatentFlow's proven pipeline (RTX 5060 8GB, cu128 nightly PyTorch)
# Adapted for Qwen3-4B-Instruct-2507 (more VRAM headroom → longer seq, bigger batch)

model:
  name: "Qwen/Qwen3-4B-Instruct-2507"
  max_seq_length: 2048
  load_in_4bit: true
  bnb_4bit_quant_type: "nf4"
  bnb_4bit_compute_dtype: "bfloat16"
  bnb_4bit_use_double_quant: true

lora:
  r: 16
  lora_alpha: 32
  lora_dropout: 0.05
  target_modules: ["q_proj", "k_proj", "v_proj", "o_proj", "gate_proj", "up_proj", "down_proj"]
  bias: "none"
  task_type: "CAUSAL_LM"

training:
  num_epochs: 3
  per_device_train_batch_size: 4
  gradient_accumulation_steps: 2  # effective batch = 8
  learning_rate: 1e-4
  lr_scheduler_type: "cosine"
  warmup_ratio: 0.05
  bf16: true
  max_grad_norm: 0.3
  gradient_checkpointing: true
  save_strategy: "steps"
  save_steps: 50
  eval_strategy: "steps"
  eval_steps: 50
  save_total_limit: 3
  load_best_model_at_end: true
  metric_for_best_model: "eval_loss"
  logging_steps: 10
  report_to: "none"

data:
  train_file: "data/train.jsonl"
  val_file: "data/val.jsonl"
  response_template: "<|im_start|>assistant\n"
  completion_only_loss: true

output:
  dir: "./outputs/feiyue-qwen3-4b-worker"
  hub_repo: "sinonchum/feiyue-qwen3-4b-worker"

# VRAM budget (RTX 5060 8GB):
# Model weights (4-bit): 2,000 MB
# LoRA + optimizer + grads: 247 MB
# Activations (grad ckpt, seq=2048, batch=4): ~1,500 MB
# CUDA overhead: 500 MB
# Total: ~4,250 MB / 8,151 MB = 52%
# Headroom: ~3,900 MB ✅
